{{ form_start(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.birthday) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.email) }}
    {{ form_row(form.confirmPassword.first) }}
    {{ form_row(form.confirmPassword.second) }}

    <button type="submit">Register!</button>
{{ form_end(form) }}